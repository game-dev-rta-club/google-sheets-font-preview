{
  "name": "google-sheets-font-preview",
  "version": "0.1.0",
  "private": true,
  "description": "Google Sheets sidebar preview for localization text with local font drag and drop.",
  "scripts": {
    "setup": "node scripts/setup-clasp.js",
    "setup-clasp": "node scripts/setup-clasp.js",
    "login-clasp": "clasp login",
    "push-clasp": "clasp push --force",
    "pull-clasp": "clasp pull",
    "open-clasp": "clasp open"
  },
  "devDependencies": {
    "@google/clasp": "^2.5.0"
  }
}
